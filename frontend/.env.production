# Configuration de PRODUCTION (utilisee par `vite build`).
# L'API et le front sont servis sur la meme origine (l'IP du serveur).
# Pour un autre serveur/domaine : modifier ces valeurs puis relancer le build,
# ou laisser deploy.sh reconstruire pour la cible.
VITE_API_URL=http://180.149.198.241/api
VITE_INTERFACE_BASE_URL=http://180.149.198.241
